$kind: http-request
method: POST
url: '{{baseUrl}}/api/items'
order: 1777103296263
description: |-
  Creates a new item. Requires Admin role.
  `category` must be one of: FOOD, MEDICAL, CLOTHING, SHELTER, WATER, EDUCATION, OTHER.
headers:
  - key: Cookie
    value: 'JSESSIONID={{token}}'
    description: Session-based auth. Admin role required.
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "name": "Winter Jacket",
      "category": "CLOTHING"
    }
